<h1>Result</h1>

<h2><%= @word.chars.all? %></h2>

<% if @result %>
  <% if @word.chars.all? %>
       <p><strong>"Congratulations! "</strong>"<%= @word %> is a valid word!"</p>
  <% else %>
      <p>Sorry but <strong><%= @word %></strong> can't be built out of #{letters}"</p>
  <% end %>
<% else %>
  <p>Sorry but <strong><%= @word %></strong> is not an English word</p>
<% end %>

<%= link_to "Play again", new_path %>
